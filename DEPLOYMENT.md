# Deployment Guide

The canonical self-hosting guide now lives in `backend/DEPLOYMENT.md`.

Use the backend copy for deployment instructions:

- `backend/deploy.sh`
- `backend/work-time-backend.service`
- `backend/DEPLOYMENT.md`
