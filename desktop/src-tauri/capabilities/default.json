{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "default",
    "description": "Default capability for the main TrackSuite.work window.",
    "windows": [
        "main"
    ],
    "permissions": [
        "core:default",
        "core:app:allow-version",
        "notification:default",
        "autostart:default",
        "updater:default"
    ]
}